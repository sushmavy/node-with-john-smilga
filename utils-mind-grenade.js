const num1 = 5;
const num2 = 10;

function addNums( a, b ){
    console.log(`Sum is ${a + b }`);
}

function subtractNums( a, b ){
    console.log(`Difference is ${a - b }`);
}

addNums( num1, num2);
subtractNums( num2, num1 );